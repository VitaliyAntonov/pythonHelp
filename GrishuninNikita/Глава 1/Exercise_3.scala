/**
 * Переменные res – это значения val или настоящие переменные var?
 * Переменные res - являются значениями val, так как их нельзя изменить, при
 * попытке присвоения res другого значения выдается ошибка error: reassignment to val
 */
object Exercise_3 {
}
